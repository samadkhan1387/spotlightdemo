name: spotlightdemo
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.1

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.8
  camera: ^0.11.0+2
  video_player: ^2.4.5
  table_calendar: ^3.0.8
  path_provider: ^2.0.5
  permission_handler: ^11.3.1
  google_fonts: ^4.0.4
  gallery_saver: ^2.3.2
  flutter_native_splash: ^2.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Lints for good coding practices
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # Adding assets (images) to your application
  assets:
    - assets/health.png
    - assets/exercise.jpg
    - assets/splash.png
    - assets/checked.png

